<%= provide(:title, @user.name) %>
<div class="row">
  <aside class="span10">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      <p>
        <% if @user.admin? && current_user?(@user) %>
          You are Admin
        <% end%>
      </p>
    </section>
  </aside>
</div>
