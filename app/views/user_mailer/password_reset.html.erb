<!DOCTYPE html>
<html>
  <%= render 'mail_head' %>
  <body>
    <div class="content">
      <%= render 'logo', header_content: "Sample Application" %>
      <h3>Hi, <%= @user.name %>!</h3>
      <p>
      To reset your password, click the URL below.
      </p>
      <a href="<%= edit_password_reset_url(@user.password_reset_token) %>">Reset password.</a>
      <p>Have a great day!</p>
      <p class="tiny">
        If you did not request your password to be reset, just ignore this email
        and your password will continue to stay the same.
      </p>
    </div>
  </body>
</html>
